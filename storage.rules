rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Lawyer uploads and reads only their own verification docs.
    match /lawyers/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
